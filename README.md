# login-system
A simple login system which runs on python shell.
If you don't have a database text file created, it will create a folder in the location where your python script is currenty running with the database text file inside.
